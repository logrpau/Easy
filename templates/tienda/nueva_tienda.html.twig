{% extends 'base.html.twig' %}


{% block title %}{{ titulo }}{% endblock %}

{% block body %}


<h2>{{ titulo }}</h2>
<br>
<br>
<br>
<br>
<div class="form-horizontal">
{{ form_start(form) }}
   {{ form_errors(form) }}
    <div class="form-group">
        <div class="control-label col-sm-2">{{ form_label(form.nombre_tienda,'Nombre de la Tienda') }}</div>
        <div class="col-sm-10">
            {{ form_widget(form.nombre_tienda, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
    <div class="form-group">
        <div class="control-label col-sm-2">{{ form_label(form.calle_tienda,'Dirección') }}</div>
        <div class="col-sm-10">
            {{ form_widget(form.calle_tienda, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
    <div class="form-group">        
      <div class="col-sm-offset-2 col-sm-10">
        <button type="submit" class="btn btn-dark">Guardar</button>
      </div>

    
{{ form_end(form) }}
</div>
</div>
{% endblock %}
